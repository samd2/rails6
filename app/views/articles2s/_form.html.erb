<%= form_with(model: articles2, local: true) do |form| %>
  <% if articles2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(articles2.errors.count, "error") %> prohibited this articles2 from being saved:</h2>

      <ul>
      <% articles2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

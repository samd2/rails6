<%= form_with(model: article3, local: true) do |form| %>
  <% if article3.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(article3.errors.count, "error") %> prohibited this article3 from being saved:</h2>

      <ul>
      <% article3.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
